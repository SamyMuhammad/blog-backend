## Simple Blog APIs built with Laravel PHP framework

## Installation Steps

* clone repository
* `composer install`
* copy `.env.example` to `.env`
* Create a DB and put its name on the `.env` file
* `php artisan key:generate`
* `php artisan migrate:fresh --seed`
* `php artisan storage:link`
